// This module contains the component registry manifest and component source files.
// Components are plain Kotlin files — no compilation needed.
// They are copied into consumer projects via the addComponent Gradle task.
